query User($filters: UsersPermissionsUserFiltersInput) {
  usersPermissionsUsers(filters: $filters) {
    data {
      id
      attributes {
        bio
        username
        email
        fullName
        avatar
        backgroundImg
        pronoun
        gender
        createdAt
        userType
      }
    }
  }
}
