query Categories($pagination: PaginationArg, $sort: [String]) {
  categories(pagination: $pagination, sort: $sort) {
    data {
      id
      attributes {
        slug
        name
      }
    }
  }
}
